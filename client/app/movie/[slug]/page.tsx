"use client";

import Link from "next/link";
import { useParams } from "next/navigation";
import { Calendar, Clock, ExternalLink, Globe, Play } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Skeleton } from "@/components/ui/skeleton";
import { ErrorState } from "@/components/common/error-state";
import { EmptyState } from "@/components/common/empty-state";
import { RatingStars } from "@/components/common/rating-stars";
import { MovieCarousel } from "@/components/movie/movie-carousel";
import { MovieActionPanel } from "@/components/movie/movie-action-panel";
import { MoviePoster } from "@/components/movie/movie-poster";
import { GenreBadge } from "@/components/movie/genre-badge";
import { ReviewCard } from "@/components/review/review-card";
import { useMovie, useMovieReviews } from "@/hooks/use-movie";
import { useMovies } from "@/hooks/use-movies";
import { formatRuntime } from "@/lib/utils";
import type { CreditRole } from "@/types";

const ROLE_LABELS: Record<CreditRole, string> = { DIRECTOR: "Director", ACTOR: "Cast", WRITER: "Writer", PRODUCER: "Producer", CINEMATOGRAPHER: "Cinematography", EDITOR: "Editor", COMPOSER: "Music" };
function MovieDetailSkeleton() { return <div className="lb-container py-8"><Skeleton className="h-96 rounded-[4px]" /></div>; }

export default function MoviePage() {
  const slug = useParams().slug as string;
  const { data: movie, isLoading, error, refetch } = useMovie(slug);
  const { data: reviewsData } = useMovieReviews(movie?.id ?? "", 1);
  const { data: relatedData } = useMovies({ page: 1, pageSize: 12 });
  if (isLoading) return <MovieDetailSkeleton />;
  if (error || !movie) return <div className="lb-container py-16"><ErrorState title="Movie not found" description="This film could not be found." retry={() => refetch()} /></div>;
  const releaseYear = movie.releaseDate ? new Date(movie.releaseDate).getFullYear() : null;
  const directors = movie.credits.filter((c) => c.role === "DIRECTOR");
  const cast = movie.credits.filter((c) => c.role === "ACTOR").slice(0, 8);
  const crew = movie.credits.filter((c) => c.role !== "ACTOR" && c.role !== "DIRECTOR");
  const genres = movie.genres.map((g) => g.genre);
  const reviews = reviewsData?.data ?? movie.reviews;
  const averageRating = movie.ratings.length ? movie.ratings.reduce((sum, r) => sum + r.rating, 0) / movie.ratings.length : movie.averageRating;
  return <div className="pb-16"><div className="relative h-56 overflow-hidden sm:h-72 lg:h-80"><MoviePoster src={movie.backdropUrl ?? movie.posterUrl} alt={movie.title} fill priority sizes="100vw" /><div className="absolute inset-0 bg-gradient-to-b from-[#0b1117]/20 via-[#0b1117]/70 to-[#0b1117]" /><div className="absolute inset-0 bg-gradient-to-r from-[#0b1117] via-[#0b1117]/50 to-transparent" /></div><div className="lb-container -mt-24 relative"><div className="grid gap-8 md:grid-cols-[220px_minmax(0,1fr)] lg:grid-cols-[250px_minmax(0,1fr)_230px]"><aside className="mx-auto w-44 md:mx-0 md:w-full"><div className="poster-shadow relative aspect-[2/3] overflow-hidden rounded-[4px] ring-1 ring-[#4a5b69]"><MoviePoster src={movie.posterUrl} alt={movie.title} fill sizes="250px" priority /></div><div className="mt-3 grid grid-cols-1 gap-2">{movie.trailers[0] && <Button asChild className="rounded-[4px] bg-[#e0362d] text-xs text-white hover:bg-[#c92e27]"><a href={movie.trailers[0].youtubeUrl} target="_blank" rel="noopener noreferrer"><Play className="h-4 w-4" />Trailer</a></Button>}</div></aside><section className="min-w-0 pt-2 md:pt-20"><h1 className="text-3xl font-semibold tracking-[-0.01em] text-[#d8e0e8] md:text-4xl">{movie.title}</h1>{movie.originalTitle && movie.originalTitle !== movie.title && <p className="mt-1 text-sm font-medium text-[#8796a6]">{movie.originalTitle}</p>}<div className="mt-3 flex flex-wrap items-center gap-x-4 gap-y-1 text-sm font-medium text-[#7e91a2]">{releaseYear && <span className="flex items-center gap-1"><Calendar className="h-3.5 w-3.5" />{releaseYear}</span>}{movie.runtimeMinutes && <span className="flex items-center gap-1"><Clock className="h-3.5 w-3.5" />{formatRuntime(movie.runtimeMinutes)}</span>}{movie.language && <span className="flex items-center gap-1"><Globe className="h-3.5 w-3.5" />{movie.language.name}</span>}{movie.country && <span>{movie.country.name}</span>}</div>{genres.length > 0 && <div className="mt-4 flex flex-wrap gap-2">{genres.map((genre) => <GenreBadge key={genre.id} genre={genre.name} href={`/explore?genre=${encodeURIComponent(genre.name)}`} />)}</div>}{movie.synopsis && <p className="mt-5 max-w-2xl text-[0.96rem] leading-7 text-[#b8c3ce]">{movie.synopsis}</p>}{directors.length > 0 && <div className="mt-5 border-t border-border-muted pt-3"><span className="lb-section-title">Directed by</span><p className="mt-1 text-sm font-semibold text-[#d8e0e8]">{directors.map((d) => d.person.fullName).join(", ")}</p></div>}{movie.sources.length > 0 && <div className="mt-5 flex flex-wrap gap-2">{movie.sources.map((source) => <a key={source.sourceUrl} href={source.sourceUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1.5 rounded-[3px] border border-[#f2c94c]/30 px-3 py-1 text-xs font-semibold text-[#f2c94c]"><ExternalLink className="h-3 w-3" />Watch on {source.sourceName}</a>)}</div>}</section><aside className="pt-2 md:pt-20"><div className="rounded-[4px] border border-border-muted bg-[#101820] p-4"><p className="lb-section-title">Ratings</p>{averageRating > 0 ? <div className="mt-3"><RatingStars rating={averageRating} size="lg" showValue valueClassName="text-2xl" /></div> : <p className="mt-3 lb-caption">No ratings yet</p>}<div className="mt-4 grid grid-cols-3 gap-2 text-center"><div><p className="text-lg font-semibold text-[#d8e0e8]">{movie.ratingsCount || movie.ratings.length}</p><p className="lb-caption">ratings</p></div><div><p className="text-lg font-semibold text-[#d8e0e8]">{reviews.length}</p><p className="lb-caption">reviews</p></div><div><p className="text-lg font-semibold text-[#d8e0e8]">{movie.trailers.length}</p><p className="lb-caption">trailers</p></div></div></div></aside></div><div className="mt-12"><Tabs defaultValue="cast"><TabsList className="rounded-[4px] border border-border-muted bg-[#101820]"><TabsTrigger value="cast">Cast & Crew</TabsTrigger><TabsTrigger value="reviews">Reviews {reviews.length > 0 && `(${reviews.length})`}</TabsTrigger>{movie.trailers.length > 0 && <TabsTrigger value="trailers">Trailers</TabsTrigger>}</TabsList><TabsContent value="cast">{cast.length > 0 && <div className="mb-6"><h3 className="lb-section-title mb-3">Cast</h3><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4">{cast.map((credit) => <Link key={credit.person.id} href={`/people/${credit.person.id}`} className="flex items-center gap-2 rounded-[4px] border border-border-muted bg-[#101820] p-2.5 hover:border-[#536575]"><div className="grid h-8 w-8 place-items-center rounded-full bg-surface-raised text-xs text-text-muted">{credit.person.fullName.charAt(0)}</div><div className="min-w-0"><p className="truncate text-xs font-medium text-foreground">{credit.person.fullName}</p>{credit.characterName && <p className="truncate text-xs text-text-muted">{credit.characterName}</p>}</div></Link>)}</div></div>}{crew.length > 0 && <div><h3 className="lb-section-title mb-3">Crew</h3><div className="grid gap-2 sm:grid-cols-2">{crew.map((credit) => <Link key={`${credit.person.id}-${credit.role}`} href={`/people/${credit.person.id}`} className="flex items-center justify-between border-b border-border-muted py-1.5 hover:border-[#536575]"><span className="text-xs text-text-muted">{ROLE_LABELS[credit.role] ?? credit.role}</span><span className="text-xs font-medium text-foreground">{credit.person.fullName}</span></Link>)}</div></div>}{cast.length === 0 && crew.length === 0 && <EmptyState title="No credits available" description="Cast and crew information coming soon." />}</TabsContent><TabsContent value="reviews"><MovieActionPanel movieId={movie.id} movieSlug={movie.slug} reviews={reviews} />{reviews.length === 0 ? <EmptyState title="No reviews yet" description="Be the first to review this film." /> : <div className="grid gap-4 sm:grid-cols-2">{reviews.map((review) => <ReviewCard key={review.id} review={review} onChanged={() => refetch()} />)}</div>}</TabsContent>{movie.trailers.length > 0 && <TabsContent value="trailers"><div className="grid gap-4 sm:grid-cols-2">{movie.trailers.map((trailer) => <div key={trailer.id} className="overflow-hidden rounded-[4px] border border-border bg-surface"><a href={trailer.youtubeUrl} target="_blank" rel="noopener noreferrer" className="flex aspect-video items-center justify-center text-sm text-accent">Watch Trailer</a><p className="px-3 py-2 text-sm text-foreground-muted">{trailer.title}</p></div>)}</div></TabsContent>}</Tabs></div>{relatedData?.data?.length ? <section className="mt-12"><div className="mb-3 lb-section-rule pt-2"><h2 className="lb-section-title">Related Films</h2></div><MovieCarousel movies={relatedData.data.filter((item) => item.id !== movie.id).slice(0, 10)} /></section> : null}</div></div>;
}

